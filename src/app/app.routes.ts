import { Routes } from '@angular/router';
import {WindowTestComponent} from './common/window-test/window-test.component';

export const WINDOW_TEST_ROUTES: Routes = [
  {
    path: '',
    component: WindowTestComponent
  }
];
